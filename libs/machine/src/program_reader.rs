use crate::Instruction;

struct ProgrammerReader{
}

impl ProgrammerReader{
    fn new(){
        file =
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn it_works() {
        assert_eq!(2, 1+1);
    }
}